# 测试指南

## ✅ 测试结果总结

### 1. 基础功能测试
- ✅ 主页可访问 (`http://localhost:3000`)
- ✅ Widget 页面可访问 (`http://localhost:3000/widget/chat?company=goldenyears`)
- ✅ FAQ Menu API 返回 200
- ✅ Chat API 返回 200

### 2. 已修复的问题
- ✅ Edge Runtime 文件系统访问问题（使用缓存的公司注册表）
- ✅ 公司 ID 验证问题

## 🧪 手动测试步骤

### 测试 1: 主页
1. 访问 `http://localhost:3000`
2. 应该看到所有公司的卡片列表
3. 每个公司有"测试页面"和"Widget 预览"按钮

### 测试 2: Demo 页面
1. 访问 `http://localhost:3000/demo/goldenyears`
2. 应该看到公司信息和 Chatbot Widget
3. 点击右下角的聊天按钮，打开聊天窗口

### 测试 3: Widget 页面（Iframe）
1. 访问 `http://localhost:3000/widget/chat?company=goldenyears`
2. 应该看到自动打开的聊天窗口
3. 尝试发送一条消息

### 测试 4: Chat API（流式响应）
使用浏览器开发者工具或 curl：

```bash
curl -X POST http://localhost:3000/api/goldenyears/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好", "sessionId": "test-123"}' \
  --no-buffer
```

应该看到流式响应数据。

### 测试 5: FAQ Menu API
```bash
curl http://localhost:3000/api/goldenyears/faq-menu
```

应该返回 JSON 格式的 FAQ 菜单数据。

## 🔧 测试命令

```bash
# 启动开发服务器
npm run dev

# 在另一个终端运行 API 测试
npm run test:api

# 测试 Supabase 连接
npm run test:supabase
```

## ⚠️ 注意事项

1. **GEMINI_API_KEY**: 
   - 需要配置有效的 Gemini API Key
   - 在 `.env.local` 中设置
   - 从 [Google AI Studio](https://makersuite.google.com/app/apikey) 获取

2. **数据库**:
   - 确保已在 Supabase 中执行 `sql/01-init.sql`
   - 运行 `npm run test:supabase` 验证连接

3. **流式响应测试**:
   - 使用 `--no-buffer` 参数查看实时流
   - 或在浏览器中使用 `fetch` API 测试

## 📝 下一步测试

1. 配置 GEMINI_API_KEY 并测试完整的 AI 对话
2. 测试流式响应的完整流程
3. 测试 Widget 在真实网站中的 iframe 集成
4. 测试多租户功能（切换不同公司）
5. 性能测试和压力测试

## 🐛 已知问题

1. **流式响应测试脚本**: 当前测试脚本可能无法完整捕获流式响应，需要手动测试
2. **知识库加载**: 在 Edge Runtime 中，知识库文件需要从 Supabase 或 CDN 加载，而不是文件系统

