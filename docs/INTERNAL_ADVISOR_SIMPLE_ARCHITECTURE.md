# 对内解惑顾问 - 简化架构设计

## 🎯 设计目标

**简单、专注、易维护** - 专门针对对内解惑顾问的架构，不包含对外客服的复杂功能。

---

## 📁 简化后的文件结构（4-5个文件）

```
knowledge/
├── 1-company_info.json         # 组织信息（必需）
├── 2-ai_config.json            # AI 配置（必需）
├── 3-knowledge_base.json       # 知识库内容（必需）
└── 4-faq.json                  # 常见问题（可选）
```

**总共：3-4 个文件**（比通用架构简单很多！）

---

## 📋 文件说明

### 1. `1-company_info.json` - 组织信息

**用途：** 存储组织基本信息、联系方式、部门信息等

**内容：**
```json
{
  "organization_name": "公司名称",
  "departments": [
    {
      "id": "hr",
      "name": "人力资源部",
      "contact": "hr@company.com"
    }
  ],
  "contact_channels": {
    "email": "info@company.com",
    "phone": "02-1234-5678"
  }
}
```

---

### 2. `2-ai_config.json` - AI 配置

**用途：** 定义意图识别和实体提取（简化版）

**内容：**
```json
{
  "intents": [
    {
      "id": "policy_inquiry",
      "keywords": ["政策", "规定", "制度"]
    },
    {
      "id": "procedure_inquiry",
      "keywords": ["流程", "步骤", "如何"]
    }
  ],
  "entity_patterns": {
    "department": [
      { "id": "hr", "keywords": ["HR", "人资", "人力资源"] }
    ]
  }
}
```

**简化：**
- ❌ 不需要复杂的优先级
- ❌ 不需要客户画像实体
- ✅ 只需要基本的意图和部门/类别实体

---

### 3. `3-knowledge_base.json` - 知识库内容（核心）

**用途：** 存储所有知识内容，AI 从这里检索回答

**结构：**
```json
{
  "articles": [
    {
      "id": "article_001",
      "title": "如何申请年假",
      "category": "hr",
      "content": "申请年假的详细流程...",
      "tags": ["年假", "请假"]
    }
  ],
  "procedures": [
    {
      "id": "proc_001",
      "title": "新员工入职流程",
      "steps": [...]
    }
  ],
  "policies": [
    {
      "id": "policy_001",
      "title": "远程办公政策",
      "content": "..."
    }
  ]
}
```

**这是核心文件！** 所有知识内容都在这里。

---

### 4. `4-faq.json` - 常见问题（可选）

**用途：** 快速回答常见问题（如果知识库检索不到，可以查 FAQ）

**简化版：**
```json
{
  "faqs": [
    {
      "id": "faq_001",
      "question": "年假可以累积吗？",
      "answer": "可以，最多累积 20 天",
      "tags": ["年假"]
    }
  ]
}
```

**注意：** 这个文件是可选的，如果知识库内容足够，可以不用 FAQ。

---

## 🔄 工作流程

### 用户提问 → AI 回答

1. **用户问：** "如何申请年假？"
2. **系统检索：** 在 `3-knowledge_base.json` 中搜索相关内容
3. **找到：** `article_001`（如何申请年假）
4. **AI 生成回答：** 基于检索到的内容生成自然语言回答

---

## 📊 对比：通用架构 vs 简化架构

| 特性 | 通用架构 | 简化架构（对内顾问） |
|------|---------|-------------------|
| **文件数量** | 7 个 | 3-4 个 |
| **配置文件** | ✅ 需要 | ❌ 不需要 |
| **服务列表** | ✅ 有 | ❌ 不需要 |
| **客户画像** | ✅ 有 | ❌ 不需要 |
| **回复模板** | ✅ 有 | ❌ 不需要 |
| **知识库** | ✅ 有 | ✅ 有（核心） |
| **FAQ** | ✅ 有 | ⚠️ 可选 |
| **复杂度** | ⭐⭐⭐⭐ | ⭐⭐ |

---

## 🎯 实施建议

### 方案 A：完全独立（推荐）

**创建新的项目目录：**
```
projects/internal-advisor/
├── config.json
└── knowledge/
    ├── 1-company_info.json
    ├── 2-ai_config.json
    ├── 3-knowledge_base.json
    └── 4-faq.json（可选）
```

**优点：**
- ✅ 结构清晰，不混淆
- ✅ 易于维护
- ✅ 可以独立优化

---

### 方案 B：复用现有结构（简化版）

**在现有项目目录下：**
```
projects/goldenyears/knowledge/
├── 2-company_info.json      # 保留
├── 3-ai_config.json         # 保留（简化）
├── 3-knowledge_base.json    # 新增（核心）
└── 5-faq_detailed.json      # 可选，简化版
```

**删除不需要的文件：**
- ❌ `1-services.json`
- ❌ `3-personas.json`
- ❌ `4-response_templates.json`
- ❌ `0-config.json`（不需要配置）

---

## 💡 推荐方案：完全独立

**创建新的项目配置：**

```
projects/internal-advisor/
├── config.json
└── knowledge/
    ├── 1-company_info.json      # 组织信息
    ├── 2-ai_config.json         # AI 配置（简化）
    └── 3-knowledge_base.json    # 知识库（核心）
```

**只需要 3 个文件！**

---

## 📝 文件内容示例

### `1-company_info.json`

```json
{
  "_file_description": "组织信息：公司/组织的基本信息、部门、联系方式",
  "organization_name": "示例公司",
  "organization_name_en": "Example Company",
  "departments": [
    {
      "id": "hr",
      "name": "人力资源部",
      "contact": "hr@company.com",
      "phone": "02-1234-5678"
    },
    {
      "id": "it",
      "name": "IT 部门",
      "contact": "it@company.com"
    }
  ],
  "contact_channels": {
    "email": "info@company.com",
    "phone": "02-1234-5678"
  }
}
```

---

### `2-ai_config.json`（简化版）

```json
{
  "_file_description": "AI 配置：意图识别和实体提取（简化版，只包含对内顾问需要的）",
  "intents": [
    {
      "id": "policy_inquiry",
      "keywords": ["政策", "规定", "制度", "规范"]
    },
    {
      "id": "procedure_inquiry",
      "keywords": ["流程", "步骤", "如何", "怎么做"]
    },
    {
      "id": "information_inquiry",
      "keywords": ["查询", "了解", "知道", "信息"]
    }
  ],
  "entity_patterns": {
    "department": [
      { "id": "hr", "keywords": ["HR", "人资", "人力资源"] },
      { "id": "it", "keywords": ["IT", "技术", "信息"] }
    ],
    "category": [
      { "id": "leave", "keywords": ["请假", "年假", "病假"] },
      { "id": "onboarding", "keywords": ["入职", "新员工"] }
    ]
  }
}
```

---

### `3-knowledge_base.json`（核心）

```json
{
  "_file_description": "知识库：所有知识内容，AI 从这里检索回答",
  "articles": [
    {
      "id": "article_001",
      "title": "如何申请年假",
      "category": "hr",
      "content": "申请年假的流程：\n1. 登录 HR 系统\n2. 选择「请假申请」\n3. 填写信息并提交\n4. 等待审批",
      "tags": ["年假", "请假", "HR"]
    }
  ],
  "procedures": [
    {
      "id": "proc_001",
      "title": "新员工入职流程",
      "category": "onboarding",
      "steps": [
        {
          "step": 1,
          "title": "准备材料",
          "description": "准备身份证、学历证明等"
        }
      ],
      "tags": ["入职", "流程"]
    }
  ],
  "policies": [
    {
      "id": "policy_001",
      "title": "远程办公政策",
      "category": "workplace",
      "content": "远程办公政策内容...",
      "tags": ["远程办公", "政策"]
    }
  ]
}
```

---

## 🎯 最终建议

### 对内解惑顾问的简化架构

**只需要 3 个文件：**

1. **`1-company_info.json`** - 组织信息
2. **`2-ai_config.json`** - AI 配置（简化版）
3. **`3-knowledge_base.json`** - 知识库内容（核心）

**工作方式：**
- 用户提问
- AI 在 `3-knowledge_base.json` 中检索
- 基于检索到的内容生成回答

**不需要：**
- ❌ 配置文件
- ❌ 服务列表
- ❌ 客户画像
- ❌ 回复模板
- ❌ 复杂的 FAQ 结构

**可选：**
- ⚠️ `4-faq.json`（如果觉得知识库检索不够，可以加一个简单的 FAQ）

---

## 📊 复杂度对比

| 架构 | 文件数 | 复杂度 | 适用场景 |
|------|--------|--------|---------|
| **通用架构** | 7 个 | ⭐⭐⭐⭐ | 需要支持多种模式 |
| **简化架构（对内）** | 3 个 | ⭐⭐ | 只做对内顾问 |

---

## 💡 总结

**对内解惑顾问的简化架构：**

```
knowledge/
├── 1-company_info.json      # 组织信息
├── 2-ai_config.json         # AI 配置（简化）
└── 3-knowledge_base.json    # 知识库（核心）
```

**总共 3 个文件，简单明了！**

**核心思想：**
- 所有知识内容放在 `3-knowledge_base.json`
- AI 从这里检索并生成回答
- 不需要预定义 QA，只需要添加知识内容
