# 好時有影知识库文件详细说明

## 📋 概述

好時有影知识库包含 **8 个 JSON 文件**，按编号组织，分为：
- **核心必需文件**（1-4）：系统运行必需
- **增强功能文件**（5-8）：提升体验和准确性

---

## 📁 文件列表

```
companies/goldenyears/knowledge/
├── 1-services.json              # 服务列表、价格
├── 2-contact_info.json          # 联系方式、分店信息
├── 3-personas.json              # AI 人格设定
├── 4-policies.json              # 公司政策、条款
├── 5-intent_config.json         # 意图识别配置
├── 6-entity_patterns.json       # 实体提取模式
├── 7-response_templates.json    # 回复模板
└── 8-faq_detailed.json          # FAQ 详细内容
```

---

## 1️⃣ 1-services.json - 服务列表与价格

### 📌 目的

**定义所有服务项目**，包括价格、描述、特色、适用人群等信息。这是系统的核心业务数据。

### 🔧 使用场景

- 价格查询时显示价格信息
- 服务推荐时匹配客户需求
- LLM 生成回复时引用服务详情
- 防止 AI 编造不存在的服务

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-01",
  "data_source": "knowledge/services.json (單一事實來源)",
  "services": [
    {
      "id": "headshot_korean",              // 服务 ID（唯一标识）
      "name": "韓式證件照",                  // 中文名称
      "name_en": "Korean Headshot",         // 英文名称
      "one_line": "...",                    // 一句话描述
      "target_audience": ["學生", ...],      // 目标客户群体
      "use_cases": ["護照", "身分證", ...],  // 使用场景
      "price_range": "約 NT$399 / 張",      // 价格范围（显示用）
      "price_min": 399,                     // 最低价格（数字，用于比较）
      "price_max": 1199,                    // 最高价格（可选）
      "price_unit": "張",                   // 价格单位
      "pricing_model": "按張計費，低消 1 張", // 计费模式说明
      "shooting_time": "約 15 分鐘...",     // 拍摄时长
      "includes_makeup": false,             // 是否包含妆发
      "retouching_count": "...",            // 修图数量说明
      "add_ons": ["妝髮服務", ...],          // 可加购项目
      "pros": ["高效率：...", ...],          // 优势特点
      "not_suitable": ["無法接受...", ...]   // 不适合的情况
    }
  ]
}
```

### 📊 当前数据

- **服务数量**：5 个
  1. `headshot_korean` - 韓式證件照 (NT$399/張)
  2. `portrait_pro` - 專業形象照 (NT$999/張)
  3. `portrait_grad_personal` - 個人寫真／畢業寫真 (NT$999-1,199/張)
  4. `group_family` - 多人合照／全家福 (NT$1,199/張)
  5. `workshop_challenge` - 一日攝影師工作坊 (NT$6,000/場)

### 💡 关键字段说明

| 字段 | 类型 | 用途 | 示例 |
|------|------|------|------|
| `id` | string | 服务唯一标识，用于代码引用 | `"headshot_korean"` |
| `price_min` | number | 用于价格比较和筛选 | `399` |
| `target_audience` | array | 用于匹配客户类型 | `["學生", "社會新鮮人"]` |
| `use_cases` | array | 用于根据用途推荐服务 | `["護照", "身分證"]` |
| `pros` / `not_suitable` | array | 用于说明服务特色和限制 | 优势列表、不适合的情况 |

---

## 2️⃣ 2-contact_info.json - 联系方式与分店信息

### 📌 目的

**存储所有联系方式**：分店地址、电话、营业时间、停车信息、社交媒体等。

### 🔧 使用场景

- 地址/联系方式查询
- 营业时间查询
- 停车信息查询
- 转接真人客服时的联系信息

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-01",
  "data_source": "knowledge/contact_info.json (單一事實來源)",
  "branches": [
    {
      "id": "zhongshan",                    // 分店 ID
      "name": "中山店",                      // 分店名称
      "address": "台北市中山區...",          // 地址
      "address_note": "近捷運中山站 2號出口", // 地址备注（交通信息）
      "phone": "02-2709-2224",              // 电话
      "hours": {
        "weekday": "週一～週日 10:00-12:30；14:00-18:30",
        "note": "不定期休"
      },
      "parking": {
        "available": true,
        "locations": ["新光三越南西店一館地下停車場", ...],
        "recommendation": "由於車位難找，建議搭乘捷運前往..."
      }
    }
  ],
  "contact_channels": {
    "email": "goldenyears166@gmail.com",
    "ig": "@goldenyears_studio",
    "line": {
      "available": false,
      "message": "不提供 Line 官方帳號..."
    },
    "booking_link": "https://www.goldenyearsphoto.com/booking/"
  },
  "ai_response_rules": {
    "line_inquiry": "我們目前沒有提供 Line 官方帳號服務...",
    "handoff_to_human": {
      "email": "...",
      "phone": {
        "zhongshan": "02-2709-2224",
        "gongguan": "02-2936-5460"
      },
      "ig": "@goldenyears_studio",
      "booking_link": "..."
    }
  }
}
```

### 📊 当前数据

- **分店数量**：2 个
  - `zhongshan` - 中山店
  - `gongguan` - 公館店

- **联系方式**：
  - Email: goldenyears166@gmail.com
  - IG: @goldenyears_studio
  - Line: 不提供（需要明确说明）

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `branches` | 分店列表 | 包含所有分店的详细信息 |
| `contact_channels` | 联系渠道 | Email、IG、Line、预约链接 |
| `ai_response_rules` | AI 回复规则 | 特殊情况的回复模板（如 Line 询问） |
| `handoff_to_human` | 转接真人信息 | 当需要转接时提供的联系方式 |

---

## 3️⃣ 3-personas.json - AI 人格设定

### 📌 目的

**定义客户画像**，帮助 AI 理解不同客户的需求、担忧、预算，从而提供个性化推荐。

### 🔧 使用场景

- 根据客户描述识别客户类型
- 推荐适合的服务
- 理解客户的担忧并针对性回应
- 提供个性化的服务建议

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-01",
  "data_source": "knowledge/personas.json (單一事實來源)",
  "personas": [
    {
      "id": "student_graduating",           // 客户画像 ID
      "name": "準畢業生（大學／碩士）",      // 客户类型名称
      "goals": ["做履歷／LinkedIn 頭像", ...], // 目标/需求
      "concerns": ["怕太貴", "怕拍出來太僵", ...], // 担忧/顾虑
      "budget_level": "中低",               // 预算水平
      "recommended_services": ["headshot_korean", "portrait_grad_personal"], // 推荐服务 ID
      "reasoning": "韓式證件照能以學生友善價格..." // 推荐理由
    }
  ]
}
```

### 📊 当前数据

- **客户画像数量**：4 个
  1. `student_graduating` - 準畢業生
  2. `pro_switching_job` - 轉職菁英
  3. `family_keeper` - 家庭主理人
  4. `hr_corporate` - 企業福委／人資窗口

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `goals` | 客户目标 | 了解客户想要达成什么 |
| `concerns` | 客户担忧 | AI 可以针对性解释和安抚 |
| `budget_level` | 预算水平 | 用于推荐合适价格的服务 |
| `recommended_services` | 推荐服务 | 该客户类型最适合的服务 ID 列表 |
| `reasoning` | 推荐理由 | 为什么推荐这些服务 |

---

## 4️⃣ 4-policies.json - 公司政策与条款

### 📌 目的

**定义公司重要政策**，确保 AI 回答合规、准确。这些政策通常是客户经常询问的敏感话题。

### 🔧 使用场景

- 政策类问题（价格、取消、隐私、授权）
- 合规性回答（确保不违反公司规定）
- 敏感话题处理（退款、投诉等）

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-01",
  "data_source": "knowledge/policies.json (單一事實來源)",
  "critical": true,                        // 标记为关键文件
  "policies": [
    {
      "id": "policy_price_scope",          // 政策 ID
      "category": "price",                 // 政策类别
      "critical": true,                    // 是否关键政策
      "question": "AI 客服可以跟我說到多細的價格資訊？", // 问题（用于匹配）
      "answer": "基本的單張價格與大約範圍...", // 标准回答
      "keywords": ["價格", "報價", ...]     // 关键词（用于匹配）
    }
  ]
}
```

### 📊 当前数据

- **政策数量**：7 个
  1. `policy_price_scope` - 价格说明范围（price）
  2. `policy_reschedule_cancel` - 改期/取消规则（booking）
  3. `policy_authorization_privacy` - 授权与隐私（privacy）
  4. `policy_edit_limit` - 修图限制（retouching）
  5. `policy_payment_methods` - 付款方式（payment）
  6. `policy_delivery_time` - 交付时间（delivery）
  7. `policy_refund` - 退费政策（refund）

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `category` | 政策类别 | 便于分类管理（price, booking, privacy 等） |
| `critical` | 是否关键 | 标记重要政策，必须严格执行 |
| `keywords` | 关键词 | 用于匹配用户问题 |
| `answer` | 标准回答 | 确保回答准确、合规 |

---

## 5️⃣ 5-intent_config.json - 意图识别配置

### 📌 目的

**配置意图识别规则**，让 AI 能够准确理解用户的意图（想要什么）。

### 🔧 使用场景

- 分析用户消息，识别用户意图
- 根据意图调用相应的处理逻辑
- 优先级排序（重要意图优先处理）

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-20",
  "data_source": "knowledge/intent_config.json (意圖分類配置)",
  "intents": [
    {
      "id": "price_inquiry",               // 意图 ID
      "priority": 3,                       // 优先级（数字越小优先级越高）
      "keywords": ["多少錢", "價格", ...],  // 匹配关键词
      "excludeKeywords": ["可以跟我說到", ...], // 排除关键词
      "contextKeywords": ["企業", "團體", ...], // 上下文关键词
      "specialConditions": {               // 特殊条件（可选）
        "shortMessage": true,
        "shortMessageThreshold": 5
      }
    }
  ],
  "fallback": {
    "useContextIntent": true,              // 是否使用上下文意图
    "contextIntentThreshold": 10,          // 上下文阈值
    "defaultIntent": "service_inquiry"     // 默认意图
  }
}
```

### 📊 当前数据

- **意图数量**：11 个（按优先级排序）
  1. `complaint` - 投诉（优先级 1，最高）
  2. `handoff_to_human` - 转接真人（优先级 2）
  3. `price_inquiry` - 价格查询（优先级 3）
  4. `delivery_inquiry` - 交付时间查询（优先级 4）
  5. `location_inquiry` - 地点/联系方式查询（优先级 5）
  6. `makeup_inquiry` - 妆发询问（优先级 6）
  7. `booking_inquiry` - 预约相关（优先级 7）
  8. `comparison` - 比较询问（优先级 8）
  9. `goodbye` - 告别（优先级 9）
  10. `greeting` - 问候（优先级 10）
  11. `service_inquiry` - 服务询问（优先级 11，默认）

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `priority` | 优先级 | 数字越小优先级越高，重要意图先匹配 |
| `keywords` | 匹配关键词 | 用户消息包含这些词时匹配该意图 |
| `excludeKeywords` | 排除关键词 | 即使匹配了 keywords，如果包含这些词则不匹配 |
| `contextKeywords` | 上下文关键词 | 结合上下文判断（如"企業報價"） |
| `specialConditions` | 特殊条件 | 如短消息特殊处理 |
| `fallback` | 回退逻辑 | 如果没有匹配到，使用默认意图 |

---

## 6️⃣ 6-entity_patterns.json - 实体提取模式

### 📌 目的

**定义实体识别规则**，从用户消息中提取结构化信息（服务类型、用途、客户类型等）。

### 🔧 使用场景

- 提取用户提到的服务类型（"我想拍形象照" → `portrait_pro`）
- 提取用途（"我要用在 LinkedIn" → `linkedin_resume`）
- 提取客户类型（"我是学生" → `student_graduating`）
- 提取分店信息（"中山店" → `zhongshan`）

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-20",
  "data_source": "knowledge/entity_patterns.json (實體提取配置)",
  "patterns": {
    "service_type": [                       // 服务类型实体
      {
        "id": "headshot_korean",
        "keywords": ["證件照", "證照", "護照照", "headshot", "韓式"]
      }
    ],
    "use_case": [                           // 使用场景实体
      {
        "id": "linkedin_resume",
        "keywords": ["履歷", "linkedin", "求職", "面試", "轉職"]
      }
    ],
    "persona": [                            // 客户类型实体
      {
        "id": "student_graduating",
        "keywords": ["學生", "畢業", "大學", "碩士"]
      }
    ],
    "branch": {                             // 分店实体（对象格式）
      "zhongshan": {
        "keywords": ["中山", "zhongshan"]
      }
    },
    "booking_action": {                     // 预约动作实体
      "book": {
        "keywords": ["預約", "book"]
      }
    }
  }
}
```

### 📊 当前数据

- **实体类型**：5 种
  1. `service_type` - 服务类型（5 个）
  2. `use_case` - 使用场景（5 个）
  3. `persona` - 客户类型（4 个）
  4. `branch` - 分店（2 个）
  5. `booking_action` - 预约动作（3 个）

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `service_type` | 服务类型 | 匹配用户提到的服务（对应 services.json 的 id） |
| `use_case` | 使用场景 | 了解用户用途，用于推荐服务 |
| `persona` | 客户类型 | 匹配客户画像（对应 personas.json 的 id） |
| `branch` | 分店 | 提取用户提到的分店（对应 contact_info.json 的 id） |
| `booking_action` | 预约动作 | 预约、改期、取消等动作 |

---

## 7️⃣ 7-response_templates.json - 回复模板

### 📌 目的

**定义标准回复模板**，确保 AI 回答的一致性、准确性和完整性。包含主要回答、补充信息、下一步建议。

### 🔧 使用场景

- 为每个意图提供标准回复模板
- 提供下一步行动建议（next_best_actions）
- 确保回复质量和一致性

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-15",
  "data_source": "knowledge/response_templates.json (單一事實來源)",
  "templates": {
    "price_inquiry": {                      // 意图 ID
      "main_answer": "我們的服務按張計費...", // 主要回答
      "supplementary_info": "所有服務都包含精修...", // 补充信息
      "next_best_actions": [                // 下一步行动建议（快速回复按钮）
        "拍攝流程",
        "妝髮建議",
        "交件時間",
        "線上預約"
      ]
    }
  }
}
```

### 📊 当前数据

- **模板数量**：11 个（对应 11 个意图）
  1. `price_inquiry` - 价格查询
  2. `booking_inquiry` - 预约相关
  3. `service_inquiry` - 服务询问
  4. `location_inquiry` - 地点查询
  5. `makeup_inquiry` - 妆发询问
  6. `makeup_comparison` - 妆发方案比较
  7. `flow_inquiry` - 流程询问
  8. `use_case_inquiry` - 用途询问
  9. `complaint` - 投诉处理
  10. `greeting` - 问候
  11. `delivery_inquiry` - 交付时间
  12. `goodbye` - 告别

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `main_answer` | 主要回答 | 直接回答用户问题，清楚完整 |
| `supplementary_info` | 补充信息 | 补充最关键的 1-2 点细节 |
| `next_best_actions` | 下一步行动 | 2-4 个常见下一步选项（会显示为快速回复按钮） |

### 🔄 格式说明

**支持两种格式：**

1. **字符串格式**（向后兼容）：
```json
{
  "templates": {
    "greeting": "您好！..."
  }
}
```

2. **对象格式**（推荐，完整功能）：
```json
{
  "templates": {
    "greeting": {
      "main_answer": "您好！...",
      "supplementary_info": "",
      "next_best_actions": ["我想了解服務項目", "如何預約"]
    }
  }
}
```

---

## 8️⃣ 8-faq_detailed.json - FAQ 详细内容

### 📌 目的

**存储详细的 FAQ 问答库**，按类别组织，用于 FAQ 菜单 API 和精确匹配用户问题。

### 🔧 使用场景

- FAQ 菜单 API（`/api/{company}/faq-menu`）
- 精确匹配用户问题
- 提供详细的问答内容

### 📐 数据结构

```json
{
  "version": "1.0.0",
  "last_updated": "2025-01-16",
  "data_source": "knowledge/faq_detailed.json (單一事實來源)",
  "categories": {
    "booking": {                            // 类别 ID
      "title": "預約問題",                   // 类别标题
      "questions": [
        {
          "id": "booking_001",              // 问题 ID
          "question": "是否可以電話預約或取消呢？", // 问题文本
          "answer": "好時有影採用線上預約制...", // 详细回答
          "keywords": ["電話預約", "取消", ...] // 关键词（用于搜索匹配）
        }
      ]
    }
  }
}
```

### 📊 当前数据

- **类别数量**：4 个
  1. `booking` - 預約問題
  2. `delivery` - 取件相關
  3. `shooting` - 拍攝相關
  4. `other` - 其他

- **问题总数**：约 40+ 个问题

### 💡 关键字段说明

| 字段 | 用途 | 说明 |
|------|------|------|
| `categories` | 分类组织 | 按主题分类，便于管理和查找 |
| `questions[].id` | 问题 ID | 唯一标识，可用于引用 |
| `questions[].keywords` | 关键词 | 用于搜索匹配用户问题 |
| `questions[].answer` | 详细回答 | 完整、准确的答案 |

---

## 🔗 文件之间的关联

### 关联关系图

```
1-services.json
    ├─→ service.id → 6-entity_patterns.json (service_type)
    └─→ service.id → 3-personas.json (recommended_services)

2-contact_info.json
    └─→ branch.id → 6-entity_patterns.json (branch)

3-personas.json
    └─→ persona.id → 6-entity_patterns.json (persona)
    └─→ recommended_services → 1-services.json (service.id)

5-intent_config.json
    └─→ intent.id → 7-response_templates.json (templates key)

6-entity_patterns.json
    ├─→ service_type.id → 1-services.json (service.id)
    ├─→ persona.id → 3-personas.json (persona.id)
    └─→ branch.key → 2-contact_info.json (branch.id)

7-response_templates.json
    └─→ templates key → 5-intent_config.json (intent.id)
```

### 数据流示例

**用户消息**："我想拍形象照，价格多少？"

1. **意图识别**（5-intent_config.json）
   - 匹配到 `price_inquiry` 意图

2. **实体提取**（6-entity_patterns.json）
   - 提取服务类型：`portrait_pro`

3. **服务信息查询**（1-services.json）
   - 查找 `portrait_pro` 的详细信息
   - 获取价格：NT$999/張

4. **回复模板**（7-response_templates.json）
   - 使用 `price_inquiry` 模板
   - 填充服务价格信息
   - 提供下一步建议

5. **生成回复**
   - 主回答：价格信息
   - 补充信息：精修说明
   - 下一步行动：["拍攝流程", "妝髮建議", ...]

---

## 📝 维护建议

### 1. 保持数据一致性

- ✅ **service.id** 必须在所有文件中保持一致
- ✅ **persona.id** 必须在所有文件中保持一致
- ✅ **intent.id** 必须与 response_templates 的 key 对应

### 2. 更新顺序

1. 先更新 `1-services.json`（核心数据）
2. 然后更新关联文件（entity_patterns, personas）
3. 最后更新模板和 FAQ

### 3. 版本管理

- 每次更新时更新 `version` 和 `last_updated`
- 保留 `data_source` 字段，明确数据来源

---

## ✅ 检查清单

添加新服务时：

- [ ] 在 `1-services.json` 中添加服务
- [ ] 在 `6-entity_patterns.json` 中添加 service_type 关键词
- [ ] 在 `3-personas.json` 中更新 recommended_services（如适用）
- [ ] 在 `7-response_templates.json` 中更新相关模板（如适用）
- [ ] 在 `8-faq_detailed.json` 中添加相关 FAQ（如适用）

添加新意图时：

- [ ] 在 `5-intent_config.json` 中添加意图配置
- [ ] 在 `7-response_templates.json` 中添加对应模板
- [ ] 确保 priority 设置合理

---

## 🎯 总结

这 8 个文件共同构成了完整的知识库系统：

1. **1-services.json** - 业务核心（服务定义）
2. **2-contact_info.json** - 联系核心（分店信息）
3. **3-personas.json** - 客户理解（个性化推荐）
4. **4-policies.json** - 合规保障（政策规范）
5. **5-intent_config.json** - 意图识别（理解用户）
6. **6-entity_patterns.json** - 实体提取（结构化信息）
7. **7-response_templates.json** - 回复标准（回答模板）
8. **8-faq_detailed.json** - 详细问答（FAQ 库）

所有文件协同工作，确保 AI 客服能够：
- ✅ 准确理解用户意图
- ✅ 提取关键信息
- ✅ 提供准确的服务信息
- ✅ 给出合适的建议
- ✅ 回答常见问题
- ✅ 保持回复一致性和合规性
