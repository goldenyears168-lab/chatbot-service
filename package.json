{
  "name": "chatbot-service-multi-tenant",
  "version": "2.0.0",
  "description": "多租户 Chatbot 微服务 - 支持多个公司",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build:css": "sass widget/src/widget.scss widget/widget.css --style=compressed",
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name=chatbot-service-multi-tenant",
    "deploy:preview": "wrangler pages deploy . --project-name=chatbot-service-multi-tenant --branch=preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build:css"
  },
  "keywords": [
    "chatbot",
    "ai",
    "microservice",
    "widget",
    "multi-tenant",
    "saas"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.94.0",
    "wrangler": "^4.51.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  }
}
